-- Optional PostgreSQL initialization.
-- With POSTGRES_USER and POSTGRES_DB provided, Postgres will create them automatically.
-- You can place seed data or additional grants here if needed.

-- Example seed
-- \connect quikbitedb
-- CREATE TABLE IF NOT EXISTS example (
--   id BIGSERIAL PRIMARY KEY,
--   name TEXT NOT NULL
-- );
