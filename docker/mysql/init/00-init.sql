-- Optional MySQL initialization.
-- With MYSQL_USER and MYSQL_DATABASE provided, MySQL will create them automatically.
-- You can place seed data or additional grants here if needed.

/* Example seed
USE quikbitedb;
CREATE TABLE IF NOT EXISTS example (
  id BIGINT PRIMARY KEY AUTO_INCREMENT,
  name VARCHAR(100) NOT NULL
);
*/
